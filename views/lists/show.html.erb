<% list_item_attributes = JSON.parse(@list[:item_attributes] || "{}").map { |_key, attribute| attribute["name"] } %>

<h1><%= @list[:name] %></h1>

<p class="mb-4"><%= @list[:description] %></p>

<div class="d-flex gap-3 mb-4">
  <a href="/lists/<%= @list[:id] %>/edit" class="btn btn-primary">edit</a>

  <form action="/lists" method="post">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="id" value="<%= @list[:id] %>" />
    <button type="submit" class="btn btn-danger">Delete</button>
  </form>
</div>

<a href="/">back to lists</a>

<form action="/lists/<%= @list[:id] %>/items" method="post" class="my-4" style="max-width: 500px">
  <div class="">
    <div class="mb-3">
      <input name="item[name]" type="text" class="form-control" placeholder="Awesome item" aria-label="Name" required>
    </div>
    <% list_item_attributes.each do |attribute| %>
      <div class="mb-3">
        <input name="attributes[<%= attribute %>]" type="text" class="form-control" placeholder="<%= attribute %>">
      </div>
    <% end %>
    <div class="text-center">
      <button type="submit" class="btn btn-primary">Create item</button>
    </div>
  </div>
</form>

<% if @items.empty? %>
  You haven't created any item for this list yet.
<% else %>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <% list_item_attributes.each do |attribute| %>
          <th><%= attribute %></th>
        <% end %>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @items.each do |item| %>
        <% item_attributes = JSON.parse(item[:attributes] || "{}") %>
        <tr>
          <td><%= item[:name] %></td>
          <% list_item_attributes.each do |attribute| %>
            <td><%= item_attributes[attribute] %></td>
          <% end %>
          <td>
            <form action="/lists/<%= @list[:id] %>/items" method="post">
              <input type="hidden" name="_method" value="DELETE">
              <input type="hidden" name="item_id" value="<%= item[:id] %>" />
              <button type="submit" class="btn btn-danger">X</button>
            </form>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
